fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
pydantic==2.10.6
python-multipart==0.0.9
anthropic==0.39.0
openai==1.54.0
httpx==0.27.2
requests==2.32.3
# python-multipart is needed for the /whatsapp Twilio webhook (Form parsing)
# requests is needed for telegram_bot.py (Telegram Bot API calls)
# httpx pinned below 0.28 -- newer httpx removes the 'proxies' arg that
# openai==1.54.0 still passes internally, causing a startup TypeError.
